#!/bin/bash

set -e
if [ ! "$1" ]; then
    echo "$0: no files specified." >&2
    exit
fi

echo -e "--- files ---"
find . -name "$1" \
    -not \( -path \*/.git -prune \) \
    -not \( -path \*.swp -prune \) \
    -not \( -path \*~ -prune \) \

echo -e "--- file contents ---"
#grep -r "$1" .
